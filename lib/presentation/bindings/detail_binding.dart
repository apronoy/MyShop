import '../../data/models/product.dart';
import '../controllers/product_controller.dart';

class DetailBinding {
  static ProductController bind(Product product, ProductController controller) {
    controller;
    return controller;
  }
}
